#########  Steps to Run the scipts ##########
1. Import the project to local workspace
2. Navigate to src/test/java/testRunner and run KarateRunner as JUnit (Junit4) Test
3. Verify the results to see JS function from external feature file executed without any issues 
4. Change the karate-core & karate-junit4 dependencies from 1.2.0 to 1.3.1
5. Repeat Step 2 and verify the results
6. Recieved ReferenceError for karate functions when used in js functions from external feature files


Refer to the attached results with this project for more details
